/*
 * This file contains a error handler function which is called after sending the error informations back to the client.
 */

function displayRuntimeError(errorId, trackingId, details) {
    alert(message);
}

function displayCommunicationError(event, jqxhr, settings, exception) {
    alert('Server communication error!');
}